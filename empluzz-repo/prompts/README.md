# prompts/

Task prompts written to be pasted into a fresh session.

| File | What it is |
|---|---|
| `session-opener.md` | Paste into a fresh session that has this repo checked out. It reads the to-do inventory back and then stops. |
| `followup-button.md` | **Not shipped.** The prompt that adds the per-row Follow-up button to the dashboard. Written 2026-08-24. The button does not exist on the live board. |

Still missing: `sweep-prompt-internship.txt` and `sweep-prompt-scholarship.txt`. They exist only inside the two desktop scheduled tasks and have to be copied out by hand. See `MIGRATION.md`.
